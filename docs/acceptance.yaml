# Acceptance Criteria
# Generated by Claude during planning phase
# Used by Codex to verify implementation and by Claude to review

criteria:
  # - description: "Feature X works as specified"
  #   test: "npm test -- --grep 'Feature X'"
  #   type: automated  # automated | manual
  #
  # - description: "No regressions in existing tests"
  #   test: "npm test"
  #   type: automated
  #
  # - description: "API response matches schema"
  #   test: "curl localhost:3000/api/health | jq ."
  #   type: manual

gate:
  lint: true
  typecheck: true
  tests: true
  format: true
  security: false  # enable for risky changes
  human_approval: false  # enable for risky changes
